extends layout

block content
  .container
    p Welcome to #{title}
    form.input-group(method='post', enctype='multipart/form-data')
      .view#record
        input#record(type='button', value='Record a Video')
        input#capture(type='file', name='videoInput', accept='video/*', capture='camcorder')
      .view#problem
        label What's the problem?
        br
        input(type='radio', name='problem', value='Police Brutality')
        label Police Brutality
        br
        input(type='radio', name='problem', value='Street Harassment')
        label Street Harassment
        br
        input(type='radio', name='problem', value='Other')
        label Other
      .view#summary
        label Summarize the problem
        br
        textarea(placeholder='Describe what you saw.')
      .view#submit
        input.btn.btn-success(type='submit', value='Submit')
